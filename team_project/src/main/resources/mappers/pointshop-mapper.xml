<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.kh.team.mappers.pointshop">
	<select id="getPoint_list" resultType="PointShopVo">
		select pno, userid, point, p_regdate, p_desc
		from tbl_point p, tbl_point_code c
		where p.p_code = c.p_code
		and userid = #{userid}
	</select>
	
	<select id="point_useList">
	</select>
</mapper>